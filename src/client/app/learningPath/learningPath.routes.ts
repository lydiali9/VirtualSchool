import { Route } from '@angular/router';
import { LearningPathComponent } from './index';

export const LearningPathRoutes: Route[] = [
  {
    path: 'browse/learningPaths',
    component: LearningPathComponent
  }
];
