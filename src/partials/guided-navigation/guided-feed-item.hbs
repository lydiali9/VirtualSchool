{{#each (lookup . 'data')}}
  <li class="feed-item align-middle {{cssClass}}">
    <a href="{{link}}">
      <h4 class="type-subtitle-xlarge title">{{title}}</h4>
    </a>
    <div class="featured-img" data-imagizer>
      <img src="{{img}}"/>
    </div>
  </li>
{{/each}}
